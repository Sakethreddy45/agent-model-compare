[project]
name = "agent-model-compare"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["amc"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]